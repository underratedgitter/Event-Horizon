# Issue Tracker: Local Markdown

Issues for this project are tracked as local markdown files under `.scratch/tickets/`.

## Format

Tickets are markdown files named `TICKET-<number>-<slug>.md` containing:
- Title
- State: (`open`, `in-progress`, `closed`)
- Triage Label
- Dependencies: blocking and blocked-by ticket references
- Specification section reference
- Verification criteria
